
      <img
        srcset="{{ section.settings.image }}"
        src="{{ section.settings.image}}"
        loading="lazy"
        alt="{{ section.settings.image.alt | escape }}">

{% schema %}
{
  "name": "home-page-banner",
  "tag": "section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:sections.image-banner.settings.image.label"
    }
  ],
  "presets": [
    {
      "name": "home-page-banner",
	  "category": "home-page"
    }
  ]
}
{% endschema %}
